{
  "name": "siatube",
  "version": "1.5.3",
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=11.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "postinstall": "npm install --prefix client && npm run build --prefix client",
    "dev": "npm start"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
